var layer = layer$glance({
    left: '/frame/left',
    main: "/photo/list",
    top: "/frame/top"
});
config;
login();
css("*,:before,:after", 'box-sizing:border-box');

var main = function () {
    return layer;
};