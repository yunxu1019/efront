var layer = layer$glance({
    left: '/frame/left',
    main: "/photos/list",
    top: "/frame/top"
});
config;
login();
css("*,:before,:after", 'box-sizing:border-box');

var main = function () {
    return layer;
};