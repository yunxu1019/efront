var buff = new Uint32Array([0x04034b50]);
decodePack(Buffer.from(buff.buffer, buff.byteOffset, buff.byteLength))
var buff = new Uint32Array([0x02014b50]);
decodePack(Buffer.from(buff.buffer, buff.byteOffset, buff.byteLength))
var buff = new Uint32Array([0x06054b50]);
decodePack(Buffer.from(buff.buffer, buff.byteOffset, buff.byteLength))